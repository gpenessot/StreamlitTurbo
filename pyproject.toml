[project]
name = "streamlit-turbo"
version = "0.1.0"
description = "Template Copier pour demarrer un projet Streamlit moderne (uv, just, ruff)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "copier>=9.10.1",
]
